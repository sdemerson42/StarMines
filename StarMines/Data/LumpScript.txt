def switch;
let counter 0;
setSpeed 40;
def dirLoop;
rand x (0-5) 5;
rand y (0-5) 5;
if x == 0 and y == 0 @dirLoop;
setDir x y;

def loop;
letPos x y;
if x > 800 or x < 0 or y > 600 or y < 0 @die;
inc counter 1;
if counter == 20 @switch;
pause;
jump @loop;

def die;
lock;
despawn;
sleep;