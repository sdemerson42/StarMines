logStr "LumpHere";
playAnim "active";
rand x (0-5) 5;
rand y (0-5) 5;
setDir x y;
let counter 0;

def loop;
inc counter 1;
ifEquals counter 100 >phase;
pause;
jump >loop;

def phase;
sendToTag "Logic" "L";
despawn;
halt;