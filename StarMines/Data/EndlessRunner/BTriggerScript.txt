def loop;
letPos x y;
if x < (0-40) @die;
pause;
jump @loop;

def die;
lock;
despawn;
sleep;

def collision;
ifCallerTag "Player" @death;
halt;

def death;
lock;
setSpeed 0;
sendToTag "Logic" "endgame";
sleep;